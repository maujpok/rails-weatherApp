<%= form_with(model: @city, id: 'add-city') do |form| %>
  <div class="form-inline">
    <%= form.text_field :name, placeholder: t('menu.add.city_name'), class: "form-control mr-sm-2", autocomplete: "off" %>
    <%= form.submit t('menu.add.add_city'), :class => 'btn' %>
  </div>
<% end %>